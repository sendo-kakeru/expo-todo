{
  "name": "expo-todo",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev:client": "turbo run dev -F=@app/client",
    "dev:api": "turbo run dev -F=@app/api",
    "dev": "turbo run dev",
    "db:migrate": "pnpm turbo db:migrate",
    "db:generate": "pnpm turbo db:generate",
    "db:studio": "pnpm turbo db:studio",
    "lint": "turbo run lint",
    "format": "turbo run fix",
    "check-types": "turbo run check-types",
    "prepare": "husky",
    "ci": "turbo run lint check-types"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.1.0",
    "prettier": "^3.5.3",
    "prettier-plugin-organize-imports": "^4.1.0",
    "prettier-plugin-tailwindcss": "^0.6.12",
    "turbo": "^2.5.4",
    "typescript": "5.8.3"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
